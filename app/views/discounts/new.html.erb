<h1>Create New Discount for <%= @merchant.name %>'s</h1>

<%= form_with model: @discounts, url: merchant_discounts_path, method: :post, local: true do |f| %>
  <%= f.hidden_field :merchant_id, value: "#{@merchant.id}" %>
  <%= f.label :percent, "Percent:" %>
  <%= f.text_field :percent%>
  <br>
  <%= f.label :threshold, "Quantity Threshold:" %>
  <%= f.text_field :threshold %>
  <br>
  <%= f.submit "Submit" %>
<% end %>